<%= form_for(@clidistrict) do |f| %>
  <% if @clidistrict.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@clidistrict.errors.count, "error") %> prohibited this clidistrict from being saved:</h2>

      <ul>
      <% @clidistrict.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :postalcode %><br />
    <%= f.text_field :postalcode %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
